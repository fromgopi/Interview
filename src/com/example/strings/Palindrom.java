package com.example.strings;

import java.util.Stack;

public class Palindrom {

	String checkPalindrom(String ip){
		Stack<Character> s = new Stack<Character>();
		
		for(int i=0;i<ip.length();i++){
			s.push(ip.charAt(i));
		}
		while(!s.isEmpty()){
			
		}
		return "";
	}
}
